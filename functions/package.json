{
  "name": "levelup-functions",
  "description": "Firebase Functions for LevelUp AI features.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1",
    "openai": "^5.0.0"
  }
}
